{
  "name": "febracis-ops",
  "version": "2.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.5.0"
  },
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "postinstall": "npm install --prefix server --omit=dev"
  },
  "dependencies": {
    "tsx": "^4.20.5"
  }
}
